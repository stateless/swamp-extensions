# JT-MINI — catalog STUB seeded from the Artificial Analysis leaderboard
# (artifacts/benchmarks/aa-leaderboard-2026-06-25.md). Open-weights; architecture/quants/self-host recipes pending
# enrichment (e.g. via the @stateless/llm-catalog `ingest` method from HF config.json).

- id: model-jt-mini
  kind: model
  name: JT-MINI
  summary: "Open-weights LLM by China Mobile; catalog stub (AA-leaderboard-seeded, 19 Intelligence Index 2026-06-25). Architecture + recipes pending."
  visibility: public
  labels: [china-mobile, open-weights, stub, aa-leaderboard]
  facets:
    aaIntelligence:
      index: 19
      basis: reference-precision # AA serves ~reference precision (provider-blended); NOT the local runsOn quant. Upper bound — read runsOn.outcome.quality for the deployed quant.
      config: reasoning
      estimated: true
      provenance:
        asOf: "2026-06-25"
        source: "Artificial Analysis leaderboard — artifacts/benchmarks/aa-leaderboard-2026-06-25.md"
        confidence: medium
  claims:
    - kind: note
      body: >-
        Leaderboard-observed (third-party, provider-dependent — NOT authoritative
        architecture): context 128k; AA Intelligence Index 19.
      provenance:
        asOf: "2026-06-25"
        source: "Artificial Analysis leaderboard — artifacts/benchmarks/aa-leaderboard-2026-06-25.md"
        confidence: medium
    - kind: note
      body: >-
        NO OPEN WEIGHTS (checked 2026-06-29): JIUTIAN (China Mobile) is API-only — no
        HF config.json/weights; param count not disclosed. Only a third-party listing
        (artificialanalysis.ai/models/jt-mini) exists. Not enrichable from HF; this stub
        stays AA-leaderboard-only unless weights are released.
      provenance:
        asOf: "2026-06-29"
        source: "https://huggingface.co/JT-LM (org checked — model absent)"
        verification: cited
        confidence: low
