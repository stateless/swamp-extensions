# Qwen3.5 family (Alibaba / Qwen). Public + generic — mined from the Qwen blog /
# HF (via the MODEL qwen35 card). Hybrid Gated-DeltaNet + attention layout: most
# layers are linear-attention DeltaNet (O(n), low KV), ~1-in-4 are full attention.
# Our-fleet eval scores / GB10 tok/s are NOT here (private). Structure: family
# umbrella + per-variant entries (variant-of → model-qwen35).

# ── family umbrella ─────────────────────────────────────────────────────────
- id: model-qwen35
  kind: model
  name: Qwen3.5 (family)
  summary: Hybrid Gated-DeltaNet + attention; thinking; multimodal; 201 languages
  visibility: public
  labels: [qwen, deltanet, thinking, multimodal]
  facets:
    architecture:
      attention: hybrid-deltanet # N × (3×(Gated DeltaNet → FFN/MoE) → 1×(Gated Attention → FFN/MoE))
      notes: "Most layers are Gated DeltaNet (linear attention, O(n)); ~1-in-4 full attention. Trained with Multi-Token Prediction (MTP)."
      multilingual: 201
      released: "2026-03-09"
      provenance:
        asOf: "2026-06-19"
        source: "https://qwen.ai/blog?id=qwen3.5"
        verification: cited
    config:
      thinking:
        control: "--reasoning-budget 0 (suppress) | -1 (unlimited)"
        minContextForThinking: 131072
      sampling: # Qwen / Unsloth recommendations
        thinkingGeneral: { temp: 1.0, top_p: 0.95, top_k: 20, presence_penalty: 1.5 }
        thinkingCoding: { temp: 0.6, top_p: 0.95, top_k: 20, presence_penalty: 0.0 }
        nonThinkingGeneral: { temp: 0.7, top_p: 0.8, top_k: 20, presence_penalty: 1.5 }
      provenance:
        asOf: "2026-06-19"
        source: "Qwen/Unsloth guidance (via MODEL qwen35 card)"
        verification: cited
  claims:
    - kind: note
      body: >-
        DeltaNet linear-attention majority → lower KV memory and higher
        throughput than an equivalent-size transformer; better long-context
        memory profile.
      provenance:
        asOf: "2026-06-19"
        source: "https://qwen.ai/blog?id=qwen3.5"
        verification: cited
    - kind: finding
      body: >-
        Quant guidance (DeltaNet-hybrid tensor sensitivity): `ssm_out` very
        sensitive (degrades sharply below Q4); `attn_*` sensitive (keep Q4+);
        `ffn_up`/`gate_exps` tolerate 3-bit. Recommended UD-Q4_K_XL / UD-Q4_K_M.
        MXFP4 retired (worse than Q4_K on many tensors). Prefer task evals over
        perplexity (PPL is not a chat/code-quality proxy).
      provenance:
        asOf: "2026-06-19"
        source: "Unsloth GGUF analysis (via MODEL qwen35 card)"
        verification: cited
    - kind: caveat
      body: >-
        Ollama incompatible (separate mmproj vision files); use llama.cpp. Think
        flag unreliable in Ollama — use `--reasoning-budget 0` or
        `--chat-template-kwargs '{"enable_thinking":false}'`. With thinking on,
        answers land in `choices[0].message.reasoning_content` (not `content`) —
        clients must read both.
      provenance:
        asOf: "2026-04-17"
        source: "MODEL qwen35 card (re-verify against primary source)"
        verification: cited
    - kind: note
      body: "License not stated in the source card — needs a primary source (HF model card)."
      provenance:
        asOf: "2026-06-19"
        source: "gap — needs primary source"
        verification: cited

# ── variants (MoE) ───────────────────────────────────────────────────────────
- id: model-qwen35-35b
  kind: model
  name: Qwen3.5-35B-A3B
  summary: MoE; leads TAU2 agentic for its size (beats the 122B); strong general
  visibility: public
  labels: [qwen, moe, agentic]
  relations:
    - { rel: variant-of, target: model-qwen35 }
  facets:
    architecture:
      attention: hybrid-deltanet
      params: "35B"
      activeParams: "3B"
      layers: 40
      experts: "8/token of 256 routed + 1 shared"
      hidden: 2048
      nativeContext: 262144
      modality: [text, image, video]
    footprint: # Unsloth-published per-quant memory (weights-only); fits a single DGX Spark
      basis: weights-only
      quants:
        - { quant: "3-bit", memGB: 17 }
        - { quant: "4-bit", memGB: 22, note: "runs on a 22GB device at 4-bit (UD-Q4)" }
        - { quant: "6-bit", memGB: 30 }
        - { quant: "8-bit", memGB: 38 }
        - { quant: BF16, memGB: 70 }
      provenance:
        asOf: "2026-06-20"
        source: "https://unsloth.ai/docs/models/qwen3.5"
        verification: cited
    benchmarks: # Qwen-published, thinking mode
      mmluPro: 85.3
      gpqaDiamond: 84.2
      ifEval: 91.9
      ifBench: 70.2
      sweBenchVerified: 69.2
      liveCodeBenchV6: 74.6
      tau2Agentic: 81.2
      mmmlu: 85.2
      provenance:
        asOf: "2026-06-19"
        source: "https://qwen.ai/blog?id=qwen3.5"
        verification: cited
  claims:
    - kind: note
      body: "Qwen3.5-Flash is the hosted form of 35B-A3B (1M context, built-in tools)."
      provenance:
        asOf: "2026-06-19"
        source: "https://qwen.ai/blog?id=qwen3.5"
        verification: cited

- id: model-qwen35-122b
  kind: model
  name: Qwen3.5-122B-A10B
  summary: Larger MoE sibling; same hybrid-deltanet layout; 256K context
  visibility: public
  labels: [qwen, moe]
  relations:
    - { rel: variant-of, target: model-qwen35 }
  facets:
    architecture:
      attention: hybrid-deltanet
      params: "122B"
      activeParams: "10B"
      nativeContext: 262144
      modality: [text, image, video]
    footprint: # weights resident; KV is GMU-dependent (vLLM allocates ~available−weights), NOT tiny at long context
      basis: weights-only
      kvGbPer1k: 0.10 # ≈27GB KV @262K at the RUNNABLE 0.85 GMU (drewid74 runbook); the 39GB@256K figure (post 398) is the 0.90 allocation, but 0.90 FAILS on GB10 — so model the config that actually runs. The ~1-in-4 full-attention layers carry KV despite the DeltaNet majority.
      quants:
        - { quant: int4, memGB: 71, note: "hybrid INT4 experts + FP8 on 144 dense layers ≈ 67-71GB weights (MEASURED: 71 @post398, 67 @drewid74 runbook; pure-INT4 ≈66GB). Single-Spark FIT is generic to INT4; the recipe's value is THROUGHPUT (28→52 tok/s) — see ap-qwen35-122b-spark-vllm. At 262K: ~67-71GB weights + ~27GB(0.85)/39GB(0.90) KV; driver reserves ~22GB, so 0.85 GMU is the safe ceiling (0.90 fails)." }
        - { quant: fp8, memGB: 122, note: "full FP8 — needs ~2 units" }
      provenance:
        asOf: "2026-06-20"
        source: "MEASURED — forum 365639 post 398 (71GB+39GB@0.90) + github.com/drewid74/optimized-qwen35-hybrid-v2-runbook-public (67GB+27GB@0.85); fp8 estimated"
        verification: cited
        confidence: medium
    benchmarks:
      mmluPro: 86.7
      gpqaDiamond: 86.6
      ifEval: 93.4
      ifBench: 76.1
      sweBenchVerified: 72.0
      liveCodeBenchV6: 78.9
      tau2Agentic: 79.5
      mmmlu: 86.7
      provenance:
        asOf: "2026-06-19"
        source: "https://qwen.ai/blog?id=qwen3.5"
        verification: cited

- id: model-qwen35-397b
  kind: model
  name: Qwen3.5-397B-A17B
  summary: Flagship MoE; hosted as Qwen3.5-Plus (1M ctx); not locally practical
  visibility: public
  labels: [qwen, moe, flagship]
  relations:
    - { rel: variant-of, target: model-qwen35 }
  facets:
    architecture:
      attention: hybrid-deltanet
      params: "397B"
      activeParams: "17B"
      layers: 60
      experts: "10/token of 512 routed + 1 shared"
      hidden: 4096
      nativeContext: 262144 # extends toward 1M for hosted
      modality: [text, image, video]
  claims:
    - kind: note
      body: "Hosted as Qwen3.5-Plus (1M context, built-in tools); ~200GB+ — multi-node for local."
      provenance:
        asOf: "2026-06-19"
        source: "https://qwen.ai/blog?id=qwen3.5"
        verification: cited

# ── variants (dense) ─────────────────────────────────────────────────────────
- id: model-qwen35-27b
  kind: model
  name: Qwen3.5-27B (dense)
  summary: Dense; 64 layers (deep for its size); competitive with 35B-A3B
  visibility: public
  labels: [qwen, dense, long-context]
  relations:
    - { rel: variant-of, target: model-qwen35 }
  facets:
    architecture:
      attention: hybrid-deltanet
      params: "27B"
      layers: 64
      hidden: 5120
      nativeContext: 262144
      modality: [text, image, video]
    footprint: # Unsloth-published per-quant memory (weights-only)
      basis: weights-only
      quants:
        - { quant: "3-bit", memGB: 14 }
        - { quant: "4-bit", memGB: 17, note: "runs on an 18GB device at 4-bit" }
        - { quant: "6-bit", memGB: 24 }
        - { quant: "8-bit", memGB: 30 }
        - { quant: BF16, memGB: 54 }
      provenance:
        asOf: "2026-06-20"
        source: "https://unsloth.ai/docs/models/qwen3.5"
        verification: cited
    benchmarks:
      mmluPro: 86.1
      gpqaDiamond: 85.5
      ifEval: 95.0
      ifBench: 76.5
      sweBenchVerified: 72.4
      liveCodeBenchV6: 80.7
      tau2Agentic: 79.0
      mmmlu: 85.9
      provenance:
        asOf: "2026-06-19"
        source: "https://qwen.ai/blog?id=qwen3.5"
        verification: cited

- id: model-qwen35-9b
  kind: model
  name: Qwen3.5-9B (dense)
  summary: Dense; ~6GB Q4; 256K context with DeltaNet — small-footprint candidate
  visibility: public
  labels: [qwen, dense, small]
  relations:
    - { rel: variant-of, target: model-qwen35 }
  facets:
    architecture:
      attention: hybrid-deltanet
      params: "9B"
      layers: 32
      hidden: 4096
      nativeContext: 262144
      modality: [text, image, video]
    footprint: # Unsloth-published per-quant memory (weights-only)
      basis: weights-only
      quants:
        - { quant: "3-bit", memGB: 5.5 }
        - { quant: "4-bit", memGB: 6.5 }
        - { quant: "6-bit", memGB: 9 }
        - { quant: "8-bit", memGB: 13 }
        - { quant: BF16, memGB: 19 }
      provenance:
        asOf: "2026-06-20"
        source: "https://unsloth.ai/docs/models/qwen3.5"
        verification: cited

# ── self-host configs: Qwen3.5-122B on DGX Spark via vLLM (forum 365639) ─────
#  LINEAGE STRATEGY: a config's LINEAR progression (Albond v1 → v2 → …) is NOT
#  enumerated as separate entries — only the current-best config is a live entry,
#  and the v1→v2 evolution lives in THIS FILE'S GIT HISTORY (`git log -p`). That
#  keeps the model files small. CONCURRENT/disagreeing variants (a different
#  contributor's cluster result) genuinely coexist, so they DO get their own
#  entry + a `replicates`/`alt-config-of` relation — git can't branch-compare.
#  (Corrects an earlier placeholder that wrongly said llama.cpp + Unsloth GGUF.)

- id: ap-qwen35-122b-spark-vllm
  kind: access-path
  name: Qwen3.5-122B · vLLM @ 1× Spark — forum 365639 v2.4 (current best)
  summary: AutoRound INT4 + FP8 shared-experts + INT8 LM head + MTP-2 (v2.4, vLLM 0.19.1-pinned); 52 avg / 55 peak tok/s single-Spark
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-122b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: uses-technique, target: technique-autoround } # quant method
    - { rel: uses-technique, target: technique-fp8 } # FP8 for shared-expert dense layers
    - { rel: uses-technique, target: technique-int8-lmhead } # INT8 LM head (Triton GEMV) — +15% (post 71)
    - { rel: uses-technique, target: technique-mtp } # MTP-2 speculative decoding (num_speculative_tokens:2)
    - { rel: uses-technique, target: technique-flashattention } # FlashInfer attention backend (+16%, drewid74 runbook)
    - { rel: built-with, target: runtime-spark-vllm-docker } # vLLM distribution for GB10 (norman.2's "community vLLM docker")
  facets:
    recipe:
      build: { runtime: vLLM, version: "0.19.1 (commit 2a69949bd) — PINNED; 0.20.1 regresses ~9% (see caveat)", image: "eugr/spark-vllm-docker (local build, tag vllm-node); also drewid74 docker runbook (cu130, torch 2.12.0)" }
      artifact: { quant: "hybrid INT4 (AutoRound) + FP8 (Qwen official, 144 shared-expert dense layers)", format: safetensors }
      hardware: { units: 1, accel: GB10 }
      technique: "v2.4: MTP-2 (num_speculative_tokens:2) + INT8 LM head (Triton GEMV) + FlashInfer backend + ~95-line vLLM INC quant patch + v2.4 backports via install.sh (triton.autotune on the head GEMV +1.2%; vLLM PR#38325 swapAB SM120 FP8 GEMM M≤64 +0.76%)"
      serving: { gpuMemoryUtilization: 0.85, attentionBackend: FLASHINFER, maxModelLen: 262144, reasoningParser: qwen3 }
      env: # GB10-mandatory (drewid74 runbook); without the first two, FlashInfer JIT OOM-kills the engine
        MAX_JOBS: "2"                                 # throttle FlashInfer JIT (else 20-32 cicc procs exhaust unified mem)
        FLASHINFER_NVCC_THREADS: "1"
        VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS: "1" # estimate (not capture) graph mem — fixes silent OOM at low GMU
        VLLM_MARLIN_USE_ATOMIC_ADD: "1"               # ~+5-7 tok/s
        PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True"
        VLLM_USE_FLASHINFER_SAMPLER: "0"
      procedure: "https://forums.developer.nvidia.com/t/365639 + production runbook github.com/drewid74/optimized-qwen35-hybrid-v2-runbook-public"
    outcome:
      speed:
        genTokS: 52 # v2.4 FINAL, single-request, short prompt — post 398 (avg 51.6±0.3, peak 54.9)
        genTokS32k: 48 # single-request at 32K filled context — post 72/396
        progression: # all single-Spark, single-request, short prompts, warm — measured (forum 365639)
          - { config: "AutoRound INT4 (v1)", tokS: 28.3 }
          - { config: "+ FP8 shared-expert layers + MTP-1 (Albond v2)", tokS: 38.4 }
          - { config: "+ INT8 LM head (shared-weight Triton GEMV)", tokS: 44, gain: "+15%", post: 71 }
          - { config: "+ MTP-2 (num_speculative_tokens:2)", tokS: 51, gain: "+33% vs 38.4", post: 71 }
          - { config: "+ v2.4 backports (triton.autotune +1.2%; PR#38325 swapAB FP8 GEMM +0.76%)", tokS: 52, note: "FINAL — avg 51.6±0.3, peak 54.9", post: 398 }
        concurrency:
          - { n: 8, aggregateTokS: 79.9, note: "MTP-2 loaded, single node — post 435" }
        range: "36.3-39.9 across Q&A/code/JSON/math/long-code (measured at the 38.4 config)"
        note: "Single-request, SHORT prompt (~30-50 tok); decays with depth — ~48 @32K (post 72/396). Decode tok/s, MTP-accounted; vLLM ~2.3x faster than Ollama."
      quality:
        measured: null # INT4-weights quality still uncertified — but the thread now HAS signal, not zero-data
        note: >-
          Attempted, inconclusive. (a) The hybrid-FP8 step is quality-neutral vs
          pure INT4 (author-verified, post 6) — but that's hybrid-vs-INT4, NOT
          INT4-vs-full-precision. (b) MTP / INT8-head are lossless/near-lossless
          (post 70). (c) A real eval (tool-eval-bench) scored 60-87/100 across
          same-config repeats — too noisy at N=15 to differentiate (post 435).
          So INT4-weights quality is still UNCERTIFIED, but no longer no-data.
        verification: unverified
      provenance: &fwd365639
        asOf: "2026-06-20"
        source: "https://forums.developer.nvidia.com/t/365639 (Albond; v2.4 final post 398)"
        verification: cited
  claims:
    - kind: note
      body: >-
        Config progression (forum 365639): 28.3 (AutoRound INT4) → 38.4 (+FP8
        shared-experts + MTP-1, Albond) → 44 (+INT8 LM head, +15%, post 71) → 51
        (+MTP-2, +33%, post 71) → 52 (v2.4 backports, FINAL, post 398). Only the
        current-best (v2.4) is a live entry; the full progression is in git history.
      provenance: *fwd365639
    - kind: finding
      body: >-
        Speculative decoding is the dominant lever: MTP-1 is the single biggest win
        (hybrid INT4+FP8 adds only ~2 tok/s atop AutoRound+MTP, 36.5→38.4); MTP-2
        adds ~16% (44→51); INT8 LM head ~+15% (38.4→44). All decode-path — footprint
        unchanged across the chain.
      provenance: *fwd365639
    - kind: finding
      body: >-
        Independently replicated: a.fairaizl reproduced ~52.0/53.3 mean (vs Albond's
        51.6 ref) on TWO different boxes — a DGX Spark Founders AND an ASUS GX10 —
        confirming the recipe ports beyond Founders hardware; ~105 tok/s aggregated
        across the two (post 431). Also topofocus 50 tok/s single node (post 426),
        norman.2 36.7-40 hybrid-only (post 9), erlendboe MTP-2>MTP-1 single Spark
        (post 28).
      provenance: *fwd365639
    - kind: finding
      body: >-
        MTP-2 (not MTP-3) is the deliberate pin: MTP-2 accept ≈80% on the 122B
        (post 71); MTP-3 gains single-stream but loses −32.9% on loaded N=8
        throughput (49.65→45.31 single, 79.91→60.13 @N=8) — a.fairaizl chose MTP-2
        on both nodes (post 435).
      provenance: *fwd365639
    - kind: caveat
      body: >-
        PIN vLLM 0.19.1 (commit 2a69949bd). v0.20.1 regresses ~9% (50.3→46.1 …
        54.2→48.5): `splitting_ops` now splits DeltaNet `gdn_attention_core` (36/48
        layers) + deepseek_v4_attention out of the decode CUDA graph → launch
        overhead. Albond froze at 0.19.1 with no plans to chase 0.20.1 (post 395/398).
      provenance: *fwd365639
    - kind: caveat
      body: >-
        Dead-ends on a single Spark: NVFP4 = 16.6 tok/s (FP4 CUTLASS missing on
        SM121, Marlin-SM80 fallback — posts 1/35); DFlash spec-decode k=5 −15% /
        k=15 −47%, accept <5%, degrades code tasks (posts 355/361); EAGLE-3 slower
        than MTP-2 on DeltaNet (post 29). MTP-2 + INT8-head remains best.
      provenance: *fwd365639
    - kind: finding
      body: >-
        Production runbook (drewid74 — builds on Albond's recipe; posted in thread
        365639): post PD-wedge-fix the ASUS GX10 (spark2) EXCEEDS the DGX Founders
        (spark1) — ~54 vs ~52 tok/s,
        262K max-len, 0.85 GMU, short prompts; per-task mean 53.3 (Q&A 53.6 / Code
        55.4 / JSON 53.9 / Math 50.0 / LongCode 57.0). Both nodes now match/exceed
        Albond's 51.6 ref. Opt stack (cumulative): FlashInfer +16%, hybrid INT4+FP8
        +8.8%, MTP-2 +25%, INT8 LM head +33%(+1.2%).
      provenance: &runbook365639
        asOf: "2026-06-20"
        source: "https://github.com/drewid74/optimized-qwen35-hybrid-v2-runbook-public"
        verification: cited
    - kind: caveat
      body: >-
        GB10 memory budget (drewid74 runbook): the CUDA driver permanently reserves
        ~22GiB → ~100GiB usable of 128. GMU 0.85 = validated safe ceiling for 262K
        max-len; 0.90 FAILS (needs 109.52GiB); 0.80 passes the budget check but
        SILENTLY OOM-kills EngineCore during CUDA-graph capture. The fix for the
        0.80 OOM is `VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=1`, NOT lowering GMU.
        Higher GMU also needs a page-cache flush + disabling gdm3 (~16GiB held).
      provenance: *runbook365639
    - kind: caveat
      body: >-
        FlashInfer JIT OOM (drewid74 runbook): without `MAX_JOBS=2` +
        `FLASHINFER_NVCC_THREADS=1`, FlashInfer's JIT spawns 20-32 parallel `cicc`
        compilers (1-4GiB each) and exhausts unified memory → OOM-kill (signal 9).
        These env vars are MANDATORY on GB10. `VLLM_MARLIN_USE_ATOMIC_ADD=1` adds
        ~5-7 tok/s. First boot 25-45 min (weights+JIT+graph); cache mounts → 7-10 min.
      provenance: *runbook365639
    - kind: caveat
      body: >-
        ASUS GX10 PD-firmware wedge (drewid74 runbook): after OOM-kills/crash-loops
        the USB-C Power-Delivery firmware can wedge — throughput 30-50% low, SM clock
        pinned at 611/708 MHz (vs 2400-2600), power <15W, NO throttle reasons shown,
        and `nvidia-smi -lgc` is accepted but ignored. Fix = AC COLD DRAIN: poweroff,
        unplug the brick from the wall 90-120s (60s insufficient), replug, restart.
        Persistent → ASUS support for PD firmware ≥5.8. (Founders units not affected.)
      provenance: *runbook365639
    - kind: note
      body: >-
        Source variance on MTP-2 accept rate: forum post 71 cites ~80%, the drewid74
        runbook cites ~95% — likely different harness/workload. Both agree MTP-2 is
        the throughput sweet spot; the accept figure is approximate.
      provenance: *runbook365639

- id: ap-qwen35-122b-spark-vllm-aoe-ray
  kind: access-path
  name: Qwen3.5-122B · vLLM Ray cluster — AoE (contributor)
  summary: AutoRound INT4 on a Ray cluster; up to 56 tok/s — DISAGREES with single-Spark
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-122b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: replicates, target: ap-qwen35-122b-spark-vllm } # concurrent variant — kept as its own entry, not git
    - { rel: uses-technique, target: technique-autoround } # quant method
    - { rel: uses-technique, target: technique-ray-serve } # Ray cluster (vs single-Spark)
    - { rel: built-with, target: runtime-spark-vllm-docker } # vLLM distribution for GB10 (Ray mode)
  facets:
    recipe:
      build: { runtime: vLLM, version: "0.19.1rc1.dev15", image: "eugr/spark-vllm-docker (local build, tag vllm-node)" }
      artifact: { quant: "INT4 (Intel/Qwen3.5-122B-A10B-int4-AutoRound)", format: safetensors }
      hardware: { config: "Ray cluster (multi-node, not single Spark)" }
      technique: "MTP (85-100% accept)"
    outcome:
      speed: { genTokS: 56, note: "Ray cluster — higher than single-Spark; explains the spread toward the title's 51" }
      provenance:
        asOf: "2026-06-19"
        source: "https://forums.developer.nvidia.com/t/365639 (AoE)"
        verification: cited
  claims:
    - kind: caveat
      body: >-
        RECONCILIATION: results span 28.3 (v1) → 36.5 (AutoRound+MTP) → 38.4
        (v2 hybrid, single Spark) → 56 (AoE Ray cluster). The title's "51 tok/s"
        sits between single-Spark v2 and the cluster — it is NOT the verified
        single-Spark number (38.4). Also: llama-benchy reports ~20 tok/s because
        it measures decode steps, not user-facing throughput — a metric
        disagreement, not a config one.
      provenance: *fwd365639

# ── eugr spark-vllm-docker recipes (DGX Spark / GB10) ────────────────────────
- id: ap-qwen35-122b-spark-vllm-fp8
  kind: access-path
  name: Qwen3.5-122B-A10B · vLLM FP8 @ 2× DGX Spark (eugr recipe)
  summary: Qwen native FP8; cluster-only (TP=2 Ray); prefix caching; 256K context
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-122b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: built-with, target: runtime-spark-vllm-docker }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: uses-technique, target: technique-fp8 }
    - { rel: uses-technique, target: technique-prefix-caching }
    - { rel: uses-technique, target: technique-tensor-parallel }
    - { rel: uses-technique, target: technique-ray-serve }
  facets:
    recipe:
      download: { hfRepo: Qwen/Qwen3.5-122B-A10B-FP8, format: safetensors }
      build: { runtime: vLLM, image: "eugr/spark-vllm-docker (local build, tag vllm-node)" }
      artifact: { quant: fp8, format: safetensors }
      hardware: { units: 2, accel: GB10 } # cluster_only: true
      mods: [mods/fix-qwen3.5-chat-template]
      command: |
        vllm serve Qwen/Qwen3.5-122B-A10B-FP8 \
          --max-model-len 262144 \
          --gpu-memory-utilization 0.8 \
          --port 8000 \
          --host 0.0.0.0 \
          --load-format fastsafetensors \
          --enable-prefix-caching \
          --enable-auto-tool-choice \
          --tool-call-parser qwen3_coder \
          --reasoning-parser qwen3 \
          --chat-template unsloth.jinja \
          -tp 2 --distributed-executor-backend ray \
          --max-num-batched-tokens 8192
      serving:
        maxModelLen: 262144
        tensorParallel: 2
        maxNumBatchedTokens: 8192
        toolCallParser: qwen3_coder
        reasoningParser: qwen3
      procedure: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-122b-fp8.yaml"
    outcome:
      context: { tokens: 262144 }
      speed: { note: "published recipe; tok/s not benchmarked in repo — measure" }
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-122b-fp8.yaml"
        verification: cited

# DUP NOTE: this is the eugr-repo pure INT4-AutoRound recipe (qwen3_xml parser,
# TP=2 Ray, no FP8/MTP). It is NOT the same config as ap-qwen35-122b-spark-vllm
# (forum 365639: hybrid INT4+FP8 + MTP-1). Different config AND different source →
# kept as its own entry with a `replicates` edge to the forum entry.
- id: ap-qwen35-122b-spark-vllm-int4-autoround
  kind: access-path
  name: Qwen3.5-122B-A10B · vLLM INT4-AutoRound @ DGX Spark (eugr recipe; solo-capable)
  summary: Intel INT4-AutoRound; fits 1× Spark (~66GB); TP=2 is the 2-Spark throughput option; qwen3_xml parser; 256K context
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-122b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: built-with, target: runtime-spark-vllm-docker }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: replicates, target: ap-qwen35-122b-spark-vllm } # forum 365639 hybrid INT4+FP8+MTP config
    - { rel: uses-technique, target: technique-autoround }
    - { rel: uses-technique, target: technique-prefix-caching }
    - { rel: uses-technique, target: technique-tensor-parallel }
    - { rel: uses-technique, target: technique-ray-serve }
  facets:
    recipe:
      download: { hfRepo: Intel/Qwen3.5-122B-A10B-int4-AutoRound, format: safetensors }
      build: { runtime: vLLM, image: "eugr/spark-vllm-docker (local build, tag vllm-node-tf5)" } # build_args: --tf5
      artifact: { quant: int4-autoround, format: safetensors }
      hardware: { units: 1, accel: GB10 } # INT4 (~66GB) fits ONE 128GB Spark — recipe flags `solo_only`; TP=2 is the throughput default for a 2-unit run, not a minimum
      mods: [mods/fix-qwen3.5-chat-template]
      env: { VLLM_MARLIN_USE_ATOMIC_ADD: "1" }
      command: |
        vllm serve Intel/Qwen3.5-122B-A10B-int4-AutoRound \
          --max-model-len 262144 \
          --gpu-memory-utilization 0.8 \
          --port 8000 \
          --host 0.0.0.0 \
          --load-format fastsafetensors \
          --enable-prefix-caching \
          --enable-auto-tool-choice \
          --tool-call-parser qwen3_xml \
          --reasoning-parser qwen3 \
          --max-num-batched-tokens 8192 \
          --trust-remote-code \
          --chat-template unsloth.jinja \
          -tp 2 \
          --distributed-executor-backend ray
      serving:
        maxModelLen: 262144
        tensorParallel: 2
        maxNumBatchedTokens: 8192
        toolCallParser: qwen3_xml
        reasoningParser: qwen3
      procedure: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-122b-int4-autoround.yaml"
    outcome:
      context: { tokens: 262144 }
      speed: { note: "published recipe; tok/s not benchmarked in repo — measure (cf. forum 365639's 38.4 t/s hybrid config)" }
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-122b-int4-autoround.yaml"
        verification: cited

- id: ap-qwen35-35b-spark-vllm-fp8
  kind: access-path
  name: Qwen3.5-35B-A3B · vLLM FP8 @ 2× DGX Spark (eugr recipe)
  summary: Qwen native FP8; TP=2 Ray; FlashInfer; prefix caching; 256K context
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-35b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: built-with, target: runtime-spark-vllm-docker }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: uses-technique, target: technique-fp8 }
    - { rel: uses-technique, target: technique-prefix-caching }
    - { rel: uses-technique, target: technique-flashattention }
    - { rel: uses-technique, target: technique-tensor-parallel }
    - { rel: uses-technique, target: technique-ray-serve }
  facets:
    recipe:
      download: { hfRepo: Qwen/Qwen3.5-35B-A3B-FP8, format: safetensors }
      build: { runtime: vLLM, image: "eugr/spark-vllm-docker (local build, tag vllm-node)" }
      artifact: { quant: fp8, format: safetensors, kvCacheDtype: fp8 }
      hardware: { units: 2, accel: GB10 }
      mods: [mods/fix-qwen3-coder-next, mods/fix-qwen3.5-chat-template]
      env: { VLLM_MARLIN_USE_ATOMIC_ADD: "1" }
      command: |
        vllm serve Qwen/Qwen3.5-35B-A3B-FP8 \
          --host 0.0.0.0 \
          --port 8000 \
          --max-model-len 262144 \
          --max-num-batched-tokens 16384 \
          --gpu-memory-utilization 0.8 \
          --enable-auto-tool-choice \
          --tool-call-parser qwen3_coder \
          --kv-cache-dtype fp8 \
          --load-format fastsafetensors \
          --attention-backend flashinfer \
          --enable-prefix-caching \
          --chat-template unsloth.jinja \
          -tp 2 \
          --distributed-executor-backend ray
      serving:
        maxModelLen: 262144
        tensorParallel: 2
        maxNumBatchedTokens: 16384
        toolCallParser: qwen3_coder
      procedure: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-35b-a3b-fp8.yaml"
    outcome:
      context: { tokens: 262144 }
      speed: { note: "published recipe; tok/s not benchmarked in repo — measure" }
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-35b-a3b-fp8.yaml"
        verification: cited

# Qwen3.5-397B INT4-AutoRound — three materially different cluster topologies:
#   2× (TP=2, --no-ray, gpu-mem-in-GB, EXPERIMENTAL), 3× (PP=3), 4× (TP=4, 32K ctx).
- id: ap-qwen35-397b-spark-vllm-int4-autoround-2x
  kind: access-path
  name: Qwen3.5-397B-A17B · vLLM INT4-AutoRound @ 2× DGX Spark (eugr EXPERIMENTAL recipe)
  summary: Intel INT4-AutoRound; TP=2, gpu-mem in GB, --no-ray; EXPERIMENTAL; 256K context
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-397b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: built-with, target: runtime-spark-vllm-docker }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: uses-technique, target: technique-autoround }
    - { rel: uses-technique, target: technique-prefix-caching }
    - { rel: uses-technique, target: technique-tensor-parallel }
    - { rel: uses-technique, target: technique-ray-serve }
  facets:
    recipe:
      download: { hfRepo: Intel/Qwen3.5-397B-A17B-int4-AutoRound, format: safetensors }
      build: { runtime: vLLM, image: "eugr/spark-vllm-docker (local build, tag vllm-node-tf5)" } # build_args: --tf5
      artifact: { quant: int4-autoround, format: safetensors, kvCacheDtype: fp8 }
      hardware: { units: 2, accel: GB10 } # cluster_only; recipe note: use with --no-ray, gpu-mem in GB
      mods: [mods/fix-qwen3.5-chat-template, mods/gpu-mem-util-gb, mods/kv-cache-prealloc-cleanup, mods/drop-caches]
      env: { VLLM_MARLIN_USE_ATOMIC_ADD: "1", VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS: "0" }
      command: |
        vllm serve Intel/Qwen3.5-397B-A17B-int4-AutoRound \
          --max-model-len 262144 \
          --max-num-seqs 2 \
          --kv-cache-dtype fp8 \
          --gpu-memory-utilization-gb 108 \
          --kv-cache-memory-bytes 2415919104 \
          --port 8000 \
          --host 0.0.0.0 \
          --enable-prefix-caching \
          --enable-auto-tool-choice \
          --tool-call-parser qwen3_xml \
          --reasoning-parser qwen3 \
          --max-num-batched-tokens 4176 \
          --trust-remote-code \
          --chat-template unsloth.jinja \
          --load-format instanttensor \
          -tp 2 \
          --distributed-executor-backend ray
      serving:
        maxModelLen: 262144
        tensorParallel: 2
        maxNumSeqs: 2
        maxNumBatchedTokens: 4176
        toolCallParser: qwen3_xml
        reasoningParser: qwen3
      procedure: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-397b-int4-autoround.yaml"
    outcome:
      context: { tokens: 262144 }
      speed: { note: "published EXPERIMENTAL recipe; tok/s not benchmarked in repo — measure" }
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-397b-int4-autoround.yaml"
        verification: cited
  claims:
    - kind: caveat
      body: >-
        EXPERIMENTAL. Set memory utilization in GB (mod gpu-mem-util-gb), not
        percentage; requires --no-ray to fit full context on two Sparks. On node
        shutdown, limit GPU clocks (`sudo nvidia-smi -lgc 200,2150`).
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.5-397b-int4-autoround.yaml"
        verification: cited

- id: ap-qwen35-397b-spark-vllm-int4-autoround-pp3
  kind: access-path
  name: Qwen3.5-397B-A17B · vLLM INT4-AutoRound @ 3× DGX Spark (PP=3, eugr 3x-cluster)
  summary: Intel INT4-AutoRound; pipeline-parallel across 3 nodes (PP=3); 256K context
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-397b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: built-with, target: runtime-spark-vllm-docker }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: uses-technique, target: technique-autoround }
    - { rel: uses-technique, target: technique-pipeline-parallel }
    - { rel: uses-technique, target: technique-prefix-caching }
    - { rel: uses-technique, target: technique-ray-serve }
  facets:
    recipe:
      download: { hfRepo: Intel/Qwen3.5-397B-A17B-int4-AutoRound, format: safetensors }
      build: { runtime: vLLM, image: "eugr/spark-vllm-docker (local build, tag vllm-node-tf5)" } # build_args: --tf5
      artifact: { quant: int4-autoround, format: safetensors, kvCacheDtype: fp8 }
      hardware: { units: 3, accel: GB10 } # 3x-spark-cluster; PP=3 mesh
      mods: [mods/fix-qwen3.5-chat-template]
      env: { VLLM_MARLIN_USE_ATOMIC_ADD: "1" }
      command: |
        vllm serve Intel/Qwen3.5-397B-A17B-int4-AutoRound \
          --max-model-len 262144 \
          --max-num-seqs 10 \
          --kv-cache-dtype fp8 \
          --gpu-memory-utilization 0.7 \
          --port 8000 \
          --host 0.0.0.0 \
          --enable-prefix-caching \
          --enable-auto-tool-choice \
          --tool-call-parser qwen3_coder \
          --reasoning-parser qwen3 \
          --max-num-batched-tokens 16384 \
          --trust-remote-code \
          --chat-template unsloth.jinja \
          -tp 1 \
          -pp 3 \
          --load-format instanttensor \
          --distributed-executor-backend ray
      serving:
        maxModelLen: 262144
        pipelineParallel: 3
        maxNumSeqs: 10
        maxNumBatchedTokens: 16384
        toolCallParser: qwen3_coder
        reasoningParser: qwen3
      procedure: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/3x-spark-cluster/qwen3.5-397b-int4-autoround.yaml"
    outcome:
      context: { tokens: 262144 }
      speed: { note: "published recipe; tok/s not benchmarked in repo — measure" }
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/3x-spark-cluster/qwen3.5-397b-int4-autoround.yaml"
        verification: cited

- id: ap-qwen35-397b-spark-vllm-int4-autoround-4x
  kind: access-path
  name: Qwen3.5-397B-A17B · vLLM INT4-AutoRound @ 4× DGX Spark (TP=4, eugr 4x-cluster)
  summary: Intel INT4-AutoRound; TP=4 across 4 nodes; 37 t/s single / 103 t/s @c4; 32K context
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-397b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: built-with, target: runtime-spark-vllm-docker }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: uses-technique, target: technique-autoround }
    - { rel: uses-technique, target: technique-prefix-caching }
    - { rel: uses-technique, target: technique-tensor-parallel }
    - { rel: uses-technique, target: technique-ray-serve }
  facets:
    recipe:
      download: { hfRepo: Intel/Qwen3.5-397B-A17B-int4-AutoRound, format: safetensors }
      build: { runtime: vLLM, image: "eugr/spark-vllm-docker (local build, tag vllm-node-tf5)" } # build_args: --tf5
      artifact: { quant: int4-autoround, format: safetensors, kvCacheDtype: fp8 }
      hardware: { units: 4, accel: GB10 } # 4x-spark-cluster; TP=4 mesh
      mods: [mods/fix-qwen3-coder-next, mods/fix-qwen35-tp4-marlin]
      env: { VLLM_MARLIN_USE_ATOMIC_ADD: "1", PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True" }
      command: |
        vllm serve Intel/Qwen3.5-397B-A17B-int4-AutoRound \
          --tool-call-parser qwen3_coder \
          --reasoning-parser qwen3 \
          --enable-auto-tool-choice \
          --tensor-parallel-size 4 \
          --distributed-executor-backend ray \
          --kv-cache-dtype fp8 \
          --gpu-memory-utilization 0.78 \
          --max-model-len 32768 \
          --max-num-batched-tokens 8192 \
          --enable-prefix-caching \
          --trust-remote-code \
          --host 0.0.0.0 \
          --port 8000
      serving:
        maxModelLen: 32768
        tensorParallel: 4
        maxNumBatchedTokens: 8192
        toolCallParser: qwen3_coder
        reasoningParser: qwen3
      procedure: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/4x-spark-cluster/qwen3.5-397b-int4-autoround.yaml"
    outcome:
      context: { tokens: 32768 }
      speed:
        genTokS: 37
        aggregateTokS: 103
        note: >-
          from recipe comment ("37 tok/s single-user, 103 tok/s aggregate (4
          concurrent) on 4× DGX Spark"), not an independent benchmark. Requires
          NVIDIA driver 580.x (590.x has a CUDAGraph deadlock bug on GB10).
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/4x-spark-cluster/qwen3.5-397b-int4-autoround.yaml"
        verification: cited

- id: ap-qwen35-397b-spark-vllm-fp8-4x
  kind: access-path
  name: Qwen3.5-397B-A17B · vLLM FP8 @ 4× DGX Spark (TP=4, eugr 4x-cluster)
  summary: Qwen native FP8; TP=4 across 4 nodes; multimodal input; 256K context
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-397b }
    - { rel: served-by, target: runtime-vllm }
    - { rel: built-with, target: runtime-spark-vllm-docker }
    - { rel: runs-on, target: hardware-dgx-spark }
    - { rel: uses-technique, target: technique-fp8 }
    - { rel: uses-technique, target: technique-prefix-caching }
    - { rel: uses-technique, target: technique-flashattention }
    - { rel: uses-technique, target: technique-tensor-parallel }
    - { rel: uses-technique, target: technique-ray-serve }
  facets:
    recipe:
      download: { hfRepo: Qwen/Qwen3.5-397B-A17B-FP8, format: safetensors }
      build: { runtime: vLLM, image: "eugr/spark-vllm-docker (local build, tag vllm-node-tf5)" } # build_args: --tf5
      artifact: { quant: fp8, format: safetensors, kvCacheDtype: fp8 }
      hardware: { units: 4, accel: GB10 } # 4x-spark-cluster; TP=4 mesh
      mods: [mods/fix-qwen3.5-autoround]
      env: { VLLM_USE_DEEP_GEMM: "0", VLLM_USE_FLASHINFER_MOE_FP16: "1", VLLM_USE_FLASHINFER_SAMPLER: "0", OMP_NUM_THREADS: "4" }
      command: |
        vllm serve Qwen/Qwen3.5-397B-A17B-FP8 \
          --max-model-len 262144 \
          --gpu-memory-utilization 0.85 \
          --port 8000 \
          --host 0.0.0.0 \
          --load-format fastsafetensors \
          --enable-prefix-caching \
          --enable-auto-tool-choice \
          --tool-call-parser qwen3_coder \
          --reasoning-parser qwen3 \
          --max-num-batched-tokens 8192 \
          --trust-remote-code \
          -tp 4 \
          --distributed-executor-backend ray \
          --mm-encoder-tp-mode data \
          --kv-cache-dtype fp8 \
          --compilation-config.cudagraph_mode none \
          --max-num-seqs 32 \
          --attention-backend flashinfer
      serving:
        maxModelLen: 262144
        tensorParallel: 4
        maxNumSeqs: 32
        maxNumBatchedTokens: 8192
        toolCallParser: qwen3_coder
        reasoningParser: qwen3
      procedure: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/4x-spark-cluster/qwen3.5-397b-a17B-fp8.yaml"
    outcome:
      context: { tokens: 262144 }
      speed: { note: "published recipe; tok/s not benchmarked in repo — measure" }
      provenance:
        asOf: "2026-06-20"
        source: "https://github.com/eugr/spark-vllm-docker/blob/main/recipes/4x-spark-cluster/qwen3.5-397b-a17B-fp8.yaml"
        verification: cited

# ── GGUF self-host access-paths (Unsloth llama.cpp; hardware-agnostic) ─────────
- id: ap-qwen35-9b-gguf-llamacpp
  kind: access-path
  name: Qwen3.5-9B · llama.cpp GGUF (Unsloth)
  summary: Unsloth GGUF (~6.5GB 4-bit); 262K context; small-footprint self-host
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-9b }
    - { rel: served-by, target: runtime-llamacpp }
    - { rel: uses-technique, target: technique-gguf-kquant }
    - { rel: uses-technique, target: technique-unsloth-dynamic }
  facets:
    recipe:
      artifact: { quant: "UD-Q4_K_XL", format: gguf, packager: unsloth, hfRepo: "unsloth/Qwen3.5-9B-GGUF" }
      command: |
        ./llama.cpp/llama-cli -hf unsloth/Qwen3.5-9B-GGUF:UD-Q4_K_XL \
          --temp 1.0 --top-p 0.95 --top-k 20
      serving: { maxModelLen: 262144 }
      note: "Unsloth Dynamic 2.0 (UD-Q2_K_XL minimum); multimodal needs --mmproj; use llama.cpp not Ollama (mmproj). thinking-general sampling."
      procedure: "https://unsloth.ai/docs/models/qwen3.5"
    outcome:
      context: { tokens: 262144 }
      provenance:
        asOf: "2026-06-20"
        source: "https://unsloth.ai/docs/models/qwen3.5"
        verification: cited

- id: ap-qwen35-35b-gguf-llamacpp
  kind: access-path
  name: Qwen3.5-35B-A3B · llama.cpp GGUF (Unsloth)
  summary: Unsloth UD-Q4 GGUF (~22GB); 262K context; single-Spark-class
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-35b }
    - { rel: served-by, target: runtime-llamacpp }
    - { rel: uses-technique, target: technique-gguf-kquant }
    - { rel: uses-technique, target: technique-unsloth-dynamic }
  facets:
    recipe:
      artifact: { quant: "UD-Q4_K_XL", format: gguf, packager: unsloth, hfRepo: "unsloth/Qwen3.5-35B-A3B-GGUF" }
      command: |
        ./llama.cpp/llama-cli -hf unsloth/Qwen3.5-35B-A3B-GGUF:UD-Q4_K_XL \
          --temp 1.0 --top-p 0.95 --top-k 20
      serving: { maxModelLen: 262144 }
      note: "runs on a 22GB device at 4-bit; multimodal needs --mmproj; use llama.cpp not Ollama. thinking-general sampling."
      procedure: "https://unsloth.ai/docs/models/qwen3.5"
    outcome:
      context: { tokens: 262144 }
      provenance:
        asOf: "2026-06-20"
        source: "https://unsloth.ai/docs/models/qwen3.5"
        verification: cited

# ── gateway access-paths (same models as the self-host paths above; real
#    OpenRouter prices, sync-refreshed). Demonstrates one model reachable BOTH
#    self-host (private GB10 measurement) and via a gateway (public cost). ─────
- id: ap-qwen35-35b-openrouter
  kind: access-path
  name: Qwen3.5-35B-A3B · OpenRouter (gateway)
  summary: Hosted access via OpenRouter
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-35b }
    - { rel: via-provider, target: provider-openrouter }
  facets:
    api: { apiSpec: openai-compat, providerModelId: "qwen/qwen3.5-35b-a3b" }
    cost:
      perMTokInUsd: 0.14
      perMTokOutUsd: 1.0
      provenance: { asOf: "2026-06-19", source: "https://openrouter.ai/api/v1/models", verification: authoritative }

- id: ap-qwen35-122b-openrouter
  kind: access-path
  name: Qwen3.5-122B-A10B · OpenRouter (gateway)
  summary: Hosted access via OpenRouter
  visibility: public
  relations:
    - { rel: of-model, target: model-qwen35-122b }
    - { rel: via-provider, target: provider-openrouter }
  facets:
    api: { apiSpec: openai-compat, providerModelId: "qwen/qwen3.5-122b-a10b" }
    cost:
      perMTokInUsd: 0.26
      perMTokOutUsd: 2.08
      provenance: { asOf: "2026-06-19", source: "https://openrouter.ai/api/v1/models", verification: authoritative }
