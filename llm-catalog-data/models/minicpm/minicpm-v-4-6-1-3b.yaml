# MiniCPM-V 4.6 1.3B — OpenBMB multimodal VLM (SigLIP2 vision + Qwen3.5-0.8B LLM).
# Enriched from the HF card + config.json (2026-06-29).

- id: model-minicpm-v-4-6-1-3b
  kind: model
  name: MiniCPM-V 4.6 1.3B
  summary: 1.3B multimodal VLM (SigLIP2-400M vision + Qwen3.5-0.8B LLM, 3:1 linear/full attn); 256K context; Apache-2.0
  visibility: public
  labels: [openbmb, open-weights, multimodal, vision, small]
  facets:
    aaIntelligence:
      index: 4
      basis: reference-precision # AA serves ~reference precision (provider-blended); NOT the local runsOn quant. Upper bound — read runsOn.outcome.quality for the deployed quant.
      config: reasoning
      estimated: false
      provenance:
        asOf: "2026-06-25"
        source: "Artificial Analysis leaderboard — artifacts/benchmarks/aa-leaderboard-2026-06-25.md"
        confidence: medium
    architecture:
      hfRepo: openbmb/MiniCPM-V-4.6
      modelType: minicpmv4_6
      params: "1.3B" # SigLIP2-400M vision + Qwen3.5-0.8B LLM
      layers: 24 # LLM; vision encoder 27
      hidden: 1024 # LLM; vision 1152
      nativeContext: 262144
      attention: "hybrid text backbone (qwen3_5_text, 3 linear-attn : 1 full-attn; GQA 8 Q / 2 KV); vision = full attn 16 heads"
      vocab: 248094
      license: apache-2.0
      modality: [text, image]
      provenance:
        asOf: "2026-06-29"
        source: "https://huggingface.co/openbmb/MiniCPM-V-4.6 config.json + model card"
        verification: cited
        confidence: medium
        note: "card body says '1B' but component sum / filename → ~1.3B; card claims video but config exposes only image+text → modality [text,image]"
    # benchmarks: card cites AA Intelligence Index 13 vs Qwen3.5-0.8B 10; no standard numeric table
