# MiniCPM5-1B — OpenBMB DENSE Llama-arch transformer (NOT an SSM hybrid). Enriched
# from the HF card + config.json (2026-06-29).

- id: model-minicpm5-1b
  kind: model
  name: MiniCPM5-1B
  summary: 1.08B dense Llama-arch transformer (GQA, hybrid-reasoning toggle); 128K context; Apache-2.0
  visibility: public
  labels: [openbmb, open-weights, dense, small]
  facets:
    aaIntelligence:
      index: 12
      basis: reference-precision # AA serves ~reference precision (provider-blended); NOT the local runsOn quant. Upper bound — read runsOn.outcome.quality for the deployed quant.
      config: reasoning
      estimated: true
      provenance:
        asOf: "2026-06-25"
        source: "Artificial Analysis leaderboard — artifacts/benchmarks/aa-leaderboard-2026-06-25.md"
        confidence: medium
    architecture:
      hfRepo: openbmb/MiniCPM5-1B
      modelType: llama
      params: "1.08B"
      layers: 24
      hidden: 1536
      nativeContext: 131072
      attention: "GQA (16 Q / 2 KV, head_dim 128); standard Llama transformer — NOT SSM/hybrid"
      vocab: 130560
      license: apache-2.0
      modality: [text]
      provenance:
        asOf: "2026-06-29"
        source: "https://huggingface.co/openbmb/MiniCPM5-1B config.json + model card"
        verification: authoritative
    # benchmarks: card claims 1B-class SOTA vs LFM2.5-1.2B-Thinking / Qwen3 but publishes no numeric table
