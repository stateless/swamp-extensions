# MiMo-V2.5 — Xiaomi omnimodal MoE. Enriched from config.json (2026-06-29).
# Benchmark table not reliably parsed → architecture only.

- id: model-mimo-v2-5
  kind: model
  name: MiMo-V2.5
  summary: 310B/15B-active omnimodal MoE (256 experts, 8/token; SWA+GA 5:1; +ViT/audio); 1M context; MIT
  visibility: public
  labels: [xiaomi, open-weights, moe, multimodal, long-context]
  facets:
    aaIntelligence:
      index: 40
      basis: reference-precision # AA serves ~reference precision (provider-blended); NOT the local runsOn quant. Upper bound — read runsOn.outcome.quality for the deployed quant.
      config: reasoning
      estimated: true
      provenance:
        asOf: "2026-06-25"
        source: "Artificial Analysis leaderboard — artifacts/benchmarks/aa-leaderboard-2026-06-25.md"
        confidence: medium
    architecture:
      hfRepo: XiaomiMiMo/MiMo-V2.5
      modelType: mimo_v2
      params: "310B"
      activeParams: "15B"
      layers: 48
      hidden: 4096
      experts: "256 routed (8/token)"
      nativeContext: 1048576
      attention: "GQA (64 / 4 KV); hybrid SWA+GA 5:1, 128 window"
      vocab: 152576
      license: mit
      modality: [text, image, video, audio] # LM backbone + 729M ViT + audio encoder
      provenance:
        asOf: "2026-06-29"
        source: "https://huggingface.co/XiaomiMiMo/MiMo-V2.5 config.json + model card (config = text LM backbone)"
        verification: authoritative
    # benchmarks: card table not reliably parsed — omitted pending direct re-read
